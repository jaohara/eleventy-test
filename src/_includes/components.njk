{% macro button(title="Button") %}
<button class="" type="button">
  {{ title }}
</button>
{% endmacro %}

{% macro tag(content="tag")%}
<span class="tag">
  {{content}}
</span>
{% endmacro %}

{# TODO: Fix this implementation and the image-box #}
{% macro image(src, alt)%}
<div class="image-wrapper">
  <img src="{{src}}" alt="{{alt}}" class="zoomable-image"/>
</div>
{% endmacro %}